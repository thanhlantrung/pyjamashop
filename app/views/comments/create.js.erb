$("#new_comment").html("Thank you for your review!");

$(".product-reviews").prepend("<%= j render @comment %>");

# this will update the average rating during the ajax.
$('.rated').attr('data-score', '<%= @product.average_rating %>');

refreshRating()