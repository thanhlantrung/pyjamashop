<div class="review-div">
	<div class="add-review-div">
	</div>
	<%= form_for([@product, @product.comments.build], remote: true) do |f| %>
	<p><div class="rating"></div></p>
	<p><%= f.text_area :body, class: "comment-box" %></p>
	<p><%= f.submit "Submit", :id => "log-in" %></p>
	<% end %>
</div>